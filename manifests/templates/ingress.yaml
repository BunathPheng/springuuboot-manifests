---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springuuboot                  # Ingress name
  labels:
    app: springuuboot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springuuboot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springuuboot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
